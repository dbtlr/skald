use std::path::Path;

use crate::error::Result;

/// Eject one or more prompt templates to the given directory.
pub fn eject_prompts(_names: &[&str], _target_dir: &Path) -> Result<Vec<String>> {
    todo!()
}
